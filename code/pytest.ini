[pytest]
asyncio_mode = auto
markers =
    unit: Fast unit tests — no network or API keys needed
    integration: Tests that hit a real DB or external API (no Gemini key needed)
    e2e: Full end-to-end agent tests — require GEMINI_API_KEY in .env
