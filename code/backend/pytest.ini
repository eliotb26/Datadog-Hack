[pytest]
asyncio_mode = auto
markers =
    unit: fast unit tests, no network or API keys required
    integration: tests that call the real Polymarket API (no auth needed)
    e2e: full end-to-end tests requiring GEMINI_API_KEY
