[pytest]
asyncio_mode = auto
markers =
    unit: fast unit tests, no network or API keys required
    integration: tests that call real APIs (Polymarket, ElevenLabs, etc.)
    e2e: full end-to-end tests requiring OPENROUTER_API_KEY

